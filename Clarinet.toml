[project]
name = "approve-robot"
authors = ["ApproveRobot Team"]
description = "Decentralized access control and permission management system"
telemetry = false
requirements = []
[contracts.auth-permissions]
path = "contracts/auth-permissions.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
